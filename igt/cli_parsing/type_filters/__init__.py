"""Composable validation and conversion filters for command-line arguments.

The subpackage separates reusable filter definitions, registry resolution, factories,
and named presets so scripts can assemble argument parsers without duplicating
low-level validation logic.
"""

